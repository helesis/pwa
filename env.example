# Voyage Sorgun Chat Backend Configuration

# Node Environment
NODE_ENV=development

# Server Port (Render otomatik PORT verir, bu sadece local için)
PORT=3000

# PostgreSQL Database URL
# Local: postgresql://user:password@localhost:5432/voyage_chat
# Render: Otomatik olarak DATABASE_URL environment variable olarak verilir
DATABASE_URL=postgresql://user:password@localhost:5432/voyage_chat

# Frontend URL (for CORS - production'da Render URL'inizi ekleyin)
# Birden fazla URL için virgülle ayırın: http://localhost:3000,https://your-app.onrender.com
FRONTEND_URL=http://localhost:3000

# Push Notification (optional)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:your-email@voyagesorgun.com

# Slack Webhook (optional - for manager alerts)
SLACK_WEBHOOK_URL=

# Email (optional - for manager alerts)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
ALERT_EMAIL=manager@voyagesorgun.com
